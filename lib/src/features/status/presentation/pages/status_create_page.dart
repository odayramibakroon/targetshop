import 'package:flutter/material.dart';

class StatusCreatePage extends StatelessWidget {
  const StatusCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}